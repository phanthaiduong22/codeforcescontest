#include <iostream>
#include <vector>
using namespace std;

int main()
{
	freopen("max.inp", "r", stdin);
	freopen("max.out", "w", stdout);
	int n, ans, x, max = 0;;
	vector<int>a;
	vector<int>b;
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		cin >> x;
		a.push_back(x);
	}
	ans = 0;
	for (int i = 2; i < n; i++)
		if ()
			cout << ans;
}